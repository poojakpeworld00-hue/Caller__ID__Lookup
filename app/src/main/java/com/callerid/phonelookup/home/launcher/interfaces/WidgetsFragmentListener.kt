package com.callerid.phonelookup.home.launcher.interfaces

import com.callerid.phonelookup.home.launcher.models.AppWidget

interface WidgetsFragmentListener {
    fun onWidgetLongPressed(appWidget: AppWidget)
}
