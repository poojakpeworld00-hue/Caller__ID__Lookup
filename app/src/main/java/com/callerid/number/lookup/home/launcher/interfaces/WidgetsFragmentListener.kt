package com.callerid.number.lookup.home.launcher.interfaces

import com.callerid.number.lookup.home.launcher.models.AppWidget

interface WidgetsFragmentListener {
    fun onWidgetLongPressed(appWidget: AppWidget)
}
