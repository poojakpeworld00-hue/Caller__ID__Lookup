<?xml version="1.0" encoding="utf-8"?>
<!-- This app's row in the mock list: white, accent border, lifted out of the list. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/hint_mock_app_bg" />
    <stroke
        android:width="2dp"
        android:color="@color/hint_accent" />
    <corners android:radius="16dp" />
</shape>
